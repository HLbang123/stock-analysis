# Cloudflare Pages config
name = "stock-alert"
compatibility_date = "2024-01-01"

# Pages project settings
[site]
bucket = "./dist"

# Build config (run by Cloudflare Pages)
[build]
command = "npm run build"
[build.environment]
NODE_VERSION = "18"
